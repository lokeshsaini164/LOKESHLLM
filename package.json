{
  "name": "lokeshllm-playwright",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "lint": "npx eslint . --ext .ts",
    "format": "npx prettier --write ."
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "eslint": "^8.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.0.0"
  }
}
